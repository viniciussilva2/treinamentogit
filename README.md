# treinamentogit

Subir arquivos !

git init 
<br>
git add . 
<br>
git commit -m "primeiro commit"
<br>
git branch -M main 
<br>
git remote add origin ( Link do repositório )
<br>
git push -u origin main 
